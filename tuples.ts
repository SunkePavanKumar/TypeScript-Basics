let students : [string, number, boolean];  // declaration of the tuples

students = ["pavan", 78, true]


// Note even we can restrict the data type need to pass in the array using tuples.But there is a case when we use the array
// method it is diminished
export{}