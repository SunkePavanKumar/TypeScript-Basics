let _id : number | string;

_id = "89"; // This will accepts both the string and the number as declared above;

// For array if we do want to pass the multiple datatypes in the single array Here we go the syntax;


let arr : Array<number | string> = []
// The different syntax will be



arr[0] = "pavan";
arr[1] = 900;
